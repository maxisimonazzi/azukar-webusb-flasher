# Puerto web expuesto en localhost
WEB_PORT=9090

# Motor de compilación: 'yowasp' (en el navegador) o 'server' (contenedor toolchain)
COMPILE_BACKEND=yowasp

# Extensiones de archivos permitidas al importar proyectos desde un archivo ZIP (separadas por coma)
ALLOWED_IMPORT_EXTENSIONS=v,pcf,txt
